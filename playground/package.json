{
  "private": true,
  "scripts": {
    "dev": "nodemon -w '../src/**/*.ts' -e .ts -x 'vite'",
    "serve":"vite",
    "build":"vite build"
  },
  "devDependencies": {
    "vite": "^2.5.3",
    "vite-plugin-inspect": "^0.2.2"
  }
}
